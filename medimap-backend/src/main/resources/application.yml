spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medimap
    username: medimap
    password: medimap

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

app:
  frontend-url: ${FRONTEND_URL}